# 🎆 烟火漫游 - 项目集成指南

## 📋 项目结构

```
Fronted/
├── src/                          # React项目（烟花引擎）
│   ├── components/
│   │   └── FireworksLanding/    # 烟花启动页组件
│   ├── utils/                   # 烟花物理引擎和渲染器
│   ├── types.ts                 # 类型定义
│   └── main.tsx
│
├── mainpage/                     # Vue 3 项目（主系统）
│   ├── src/
│   │   ├── views/
│   │   │   ├── Landing.vue      # ✨ 烟花启动页（新）
│   │   │   ├── MainLayout.vue
│   │   │   ├── MapView.vue
│   │   │   └── ... 其他页面
│   │   ├── router/index.js      # 路由配置
│   │   ├── App.vue
│   │   └── main.js
│   ├── vite.config.js           # Vite配置（含烟花引擎别名）
│   └── package.json
│
├── package.json                  # React项目依赖
├── vite.config.ts               # React项目配置
└── tsconfig.json
```

---

## 🚀 快速启动

### 方案A：只运行 mainpage（推荐用于生产）

```bash
cd Fronted/mainpage

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 或构建生产版本
npm run build
npm run preview
```

**访问**: http://localhost:5173/
- 首页显示：烟花启动页（Loading.vue）
- 点击"进入系统"：跳转到 `/main/map`
- 主系统正常运行

---

### 方案B：同时运行两个项目（用于开发）

**终端1 - 启动 mainpage（Vue项目）**:
```bash
cd Fronted/mainpage
npm install
npm run dev
# 访问: http://localhost:5173/
```

**终端2 - 启动 src 项目（React项目）** （可选，仅用于调试烟花引擎）:
```bash
cd Fronted
npm install
npm run dev
# 访问: http://localhost:5173 (默认端口可能冲突，会自动使用其他端口)
```

---

## 🎯 工作流程

### 用户访问流程
```
1. 进入页面 → http://localhost:5173/
2. 显示烟花启动页 (Landing.vue)
3. 用户观看烟花效果（约2.5秒后可交互）
4. 点击"进入系统"或按Enter
5. 页面淡出，跳转到 /main/map
6. 显示主系统 (MapView.vue)
```

### 文件修改清单

✅ **已完成**：
- 删除了队友的旧 Landing.vue
- 创建了新的烟花 Landing.vue（Vue 3版本）
- 配置了烟花引擎的别名导入
- 集成了烟花启动页的"进入系统"按钮跳转逻辑

---

## 🔧 配置说明

### vite.config.js（mainpage）

```javascript
resolve: {
  alias: {
    '@': resolve(__dirname, 'src'),
    '@fireworks': resolve(__dirname, '../src')  // 烟花引擎别名
  }
}
```

这样 Landing.vue 可以通过 `@fireworks/utils/...` 导入烟花引擎。

---

## 🎨 烟花启动页配置

### 修改启动页样式

编辑 `mainpage/src/views/Landing.vue` 的 `<style>` 部分：

```vue
<!-- 修改背景颜色 -->
.landing-container {
  background: linear-gradient(135deg, #faf3e8 0%, #fff5e6 100%);
}

<!-- 修改标题颜色 -->
.title-main {
  color: #8b2a42;  /* 改为你想要的颜色 */
}

<!-- 修改按钮样式 -->
.menu-button {
  background: #8b2a42;   /* 按钮背景色 */
  border: 2px solid #d4af37;  /* 边框色 */
}
```

### 修改启动页文字

编辑 `mainpage/src/views/Landing.vue` 的 `<template>` 部分：

```vue
<!-- 修改标题 -->
<span class="title-char char-1">你</span>
<span class="title-char char-2">的</span>
<span class="title-char char-3">标</span>
<span class="title-char char-4">题</span>

<!-- 修改英文副标题 -->
<h2 class="title-en">Your Subtitle</h2>

<!-- 修改按钮文字 -->
<span class="button-text">自定义按钮文本</span>
```

---

## 📊 技术栈

### React项目 (src/)
- React 18
- TypeScript
- Canvas API
- Vite

### Vue项目 (mainpage/)
- Vue 3
- TypeScript
- Vue Router 4
- Pinia（状态管理）
- Element Plus（UI框架）
- Tailwind CSS
- Cesium（地图库）

---

## 🐛 常见问题

### Q1: 烟花动画不显示？
**A**: 检查浏览器控制台是否有错误。如果加载失败，会自动降级到简化版本（简单粒子效果）。

### Q2: 点击按钮没有跳转？
**A**: 检查路由配置是否正确：
```javascript
// mainpage/src/router/index.js
{
  path: '/main',
  name: 'Main',
  component: () => import('@/views/MainLayout.vue'),
  children: [
    { path: 'map', name: 'Map', component: () => import('@/views/MapView.vue') }
  ]
}
```

### Q3: 如何修改跳转的目标页面？
**A**: 编辑 `mainpage/src/views/Landing.vue` 的 handleEnter 函数：
```javascript
const handleEnter = () => {
  // 改为你想要的路由
  router.push('/main/route')  // 改为其他页面
}
```

### Q4: 两个项目怎么同步代码到GitHub？
**A**: 
```bash
# 在根目录
git add .
git commit -m "集成烟花启动页"
git push origin [your-branch]
```

---

## ✨ 功能演示

| 功能 | 位置 | 说明 |
|------|------|------|
| 烟花动画 | Landing.vue canvas | 真实物理引擎烟花 |
| 启动页UI | Landing.vue template | 标题、装饰、按钮 |
| 路由跳转 | Landing.vue router.push() | 点击后进入主系统 |
| 主系统 | MainLayout.vue + 子路由 | 地图、路线、足迹等 |

---

## 📝 Git提交日志

```
[日期] 集成烟花启动页
- ✅ 删除旧的Landing.vue
- ✅ 创建烟花启动页（Vue 3版本）
- ✅ 配置烟花引擎别名
- ✅ 集成路由跳转
- ✅ 测试通过
```

---

**准备好了！现在可以启动项目了** 🚀

